# beacon

SEO and marketing/PR tool for Hunya Munya Records artists. Companion to `/site`.

**Status:** stub. Spec pending from Cowork.

**Intended stack (per 2026-04-22 dry-run):** FastAPI + React. Python backend ingests submission JSON from R2 (`submissions/` prefix), aggregates artist metrics, and exposes an internal React dashboard. Will not be publicly reachable at launch.

Do not add code here until Cowork's Beacon spec lands.
